/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "print.h"
/*This function takes as argument pointer to the structure
 *This function is used to get the input from the user and store them in an array 
 */
void
input_list_values(a2_sort *formal_list)
{
	int count;
	int list_item;
	int i;
	printf("enter the number of elements: \n");
	scanf("%d", &count);
	formal_list->count = count;
	printf("enter the list elements: \n");
		for(i = 0; i < count; i++)
		{	
		printf("\n enter the list[%d] element: \n", i+1);
		scanf("%d", &list_item);
		/*Assigning the elements to the structure member*/
		formal_list->list[i] = list_item;
		}
		

}
/*This function is used to get the values for both the matrices
 *The argument is a structure pointer
 */
void
input_array_values(a2_matrix_mul *formal_array)
{
	int row1;
	int col1;
	int row2;
	int col2;
	int formal_first[100];
	int formal_second[100];
	int i,j;
	int z = 0;
	int k = 0;
	printf("\n Enter the Dimension(n) of the Matrix: \n");
	scanf("%d", &row1);
	/*Assigning the values of rows and columns to the structure member*/
	formal_array->row_first = row1;
	formal_array->col_first = row1;
	for(i = 0; i < row1; i++)
	{
		for(j = 0; j < row1; j++)
		{
			printf("Enter the value of matrix[%d][%d]: \n", i,j);
			scanf("%d", &formal_first[z]);
		/*Assigning the array elements to the structure member*/			
			formal_array->first[z] = formal_first[z];		
			z++;
		}
	}
	z = 0;
	/*Assigning the values of rows and columns to the structure*/
	formal_array->row_second = row1;
	formal_array->col_second = row1;
	printf("\nEnter the values for second Matrix\n");
	for(i = 0; i < row1; i++)
	{
		for(j = 0; j < row1; j++)
		{
			printf("Enter the value of matrix[%d][%d]: \n", i,j);
			scanf("%d", &formal_second[k]);
			/*Assigning the array elements to the structure member*/
			formal_array->second[k] = formal_second[k];
			k++;
		}
	}
	k = 0;
	
	/*for(i = 0; i < row1*row1; i++)
	{
	
		printf("%d\t", formal_first[i]);
		printf("%d\t", formal_second[i]);
	}*/
}


void
morampudi_a2_1(char *host)
{
	CLIENT *clnt;
	enum clnt_stat retval_1;
	a2_echo result_1;
	a2_echo  echo_1_arg;
	a2_sort *formal_sorted_list;
	int formal_count;
	enum clnt_stat retval_2;
	a2_sort result_2;
	a2_sort  sort_1_arg;
	int list_count;
	enum clnt_stat retval_3;
	a2_path result_3;
	char *path_1_arg;
	int i;
	int count1;
	char entered_input[100];
	enum clnt_stat retval_4;
	a2_file_check result_4;
	a2_file_check  file_check_1_arg;
	char file_name[100];
	enum clnt_stat retval_5;
	a2_matrix_mul result_5;
	a2_matrix_mul  matrix_mul_1_arg;
	int result_matrix[100][100];
	a2_matrix_mul *formal_result;
	int row;
	int col;
	int j;
	int z = 0;
	
#ifndef	DEBUG
	clnt = clnt_create (host, Morampudi_a2, a2Vers, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror (host);
		exit (1);
	}
#endif	/* DEBUG */
/*When the user types option 6, the program exits*/
while(i!=6)
{	/*Menu for the application*/
	printf("\n");
	printf("1. Echo the Input\n");
	printf("2. Sort the list\n");
	printf("3. Output the Present working directory\n");
	printf("4. Check if a file is available\n");
	printf("5. Matrix Multiplication\n");
	printf("6. Exit\n");
	printf("\n Please choose an option:\n");
	scanf("%d", &i);

     
     
	switch(i)
	{	
	
	/*case 1 is the Echo function*/
	case 1:
	
	printf("\n");
	printf("enter the input: \n");
	scanf("%s", &entered_input);
	/*copying the user input into the argument passed to the function*/
	strcpy(echo_1_arg.input, entered_input);
	/*this function is generated by rpcgen*/
	retval_1 = echo_1(&echo_1_arg, &result_1, clnt);
	if (retval_1 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
	/*If there is no error during communication, the client prints the message received from the server*/
	else{
	printf("\n");
	printf("This is the result: %s \n", result_1.input);
	}
	break;

	/*case 2 is for sorting the list elements*/	
	case 2:
	/*function to get the list elements from the user input*/
	input_list_values(&sort_1_arg);
	/*This function is generate by rpcgen*/
	retval_2 = sort_1(&sort_1_arg, &result_2, clnt);
	if (retval_2 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
	/*If there is no error in the system, then the sorted output is displayed to the user*/
	else{
	printf("Sorted List: \n");
	/*storing the result structure in the pointer to structure for easy access of variables*/
	formal_sorted_list = &result_2;
	/*assigning the local variable the count of elements in the list*/
	formal_count = formal_sorted_list->count;
	for(i = 0; i < formal_count; i++)
	{
	/*Printing the sorted list elements*/
	printf("\n  %d \n", formal_sorted_list->sorted_list[i]);

	}
	
	
	
	}
	break;
	
	/*case 3 is the option for displaying the current working directory*/
	case 3:
	/*Function generated by rpcgen*/
	retval_3 = path_1((void*)&path_1_arg, &result_3, clnt);
	if (retval_3 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
	else
	{
	
	printf("This is the working directory: %s \n\n", result_3.pwd);
	printf("\n");
	
	}
	break;
	/*Case 4 is for checking if the entered filename is present at the server*/
	case 4:
	printf("\nEnter the Filename:\n");

	scanf("%s", &file_name);
	/*Copying the entered filename into the function argument from the local variable file_name*/
	strcpy(file_check_1_arg.files, file_name);	
	/*function generated by rpcgen*/
	retval_4 = file_check_1(&file_check_1_arg, &result_4, clnt);
	if (retval_4 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
	else{
	
	printf("Requested file:  %s\n", result_4.files);
	}
	break;
	/*case 5 is for matrix multiplication*/
	case 5:
	/*Function to get the matrix values into the system*/
	input_array_values(&matrix_mul_1_arg);
	retval_5 = matrix_mul_1(&matrix_mul_1_arg, &result_5, clnt);
	if (retval_5 != RPC_SUCCESS) {
		clnt_perror (clnt, "call failed");
	}
	else{
	/*storing the result structure in a pointer to structure*/
	formal_result = &result_5;
	/*storing the values of row and columns in the local variables*/
	row = formal_result->row_first;
	col = formal_result->col_second;
	for(i = 0; i < row; i++)
	{
		for(j = 0; j < row; j++)
		{
		/*De Serializing the array values received from the server and storing it in two dimensional array*/
		result_matrix[i][j] = formal_result->multiplied[z];
		z++;
		}
	}
	/*reassigning the value of z to 0 in order to keep the global variable not altered for next call*/
	z=0;
	printf("Result Matrix is: \n");
	for(i = 0; i < row; i++ )
	{
		for(j = 0; j < row; j++)
		{
		
		
		printf("%d\t", result_matrix[i][j]);
		}
		printf("\n");
	}
	
	default:
	printf("\nPlease enter a valid option!\n");
	break;	
	
	
	
	}
	
	
	/*closing brace for switch case, this comment is for Developers readability*/
	}
}  

	


   
#ifndef	DEBUG
	clnt_destroy (clnt);
#endif	 /* DEBUG */
}

/*Main Method*/
int
main (int argc, char *argv[])
{
	char *host;

	if (argc < 2) {
		printf ("usage: %s server_host\n", argv[0]);
		exit (1);
	}
	host = argv[1];
	morampudi_a2_1 (host);
exit (0);
}
